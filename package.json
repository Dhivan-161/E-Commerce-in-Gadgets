{
  "name": "1battery-root",
  "version": "1.0.0",
  "description": "Root package to redirect npm commands to the 1% Battery folder",
  "scripts": {
    "start": "npm run dev --prefix GadgetHub",
    "dev": "npm run dev --prefix GadgetHub",
    "build": "npm run build --prefix GadgetHub",
    "install": "npm install --prefix GadgetHub"
  }
}
